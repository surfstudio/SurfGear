name: surf_util
description: Common classes and util.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  logger:
    git:
      url: https://bitbucket.org/surfstudio/flutter-standard.git
      ref: dev
      path: logger

  mwwm:
    git:
      url: https://bitbucket.org/surfstudio/flutter-standard.git
      ref: dev
      path: mwwm

  swipe_refresh:
    git:
      url: https://bitbucket.org/surfstudio/flutter-standard.git
      ref: dev
      path: swipe_refresh

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
