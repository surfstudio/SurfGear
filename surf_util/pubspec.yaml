name: surf_util
description: Common classes and util.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  logger:
    git:
      url: https://bitbucket.org/surfstudio/flutter-standard.git
      ref: project-ROS-25
      path: logger

  mwwm:
    git:
      url: https://bitbucket.org/surfstudio/flutter-standard.git
      ref: project-ROS-25
      path: mwwm

  ## todo: заменить на другой путь когда будет в dev
  swipe_refresh:
    path: ../swipe_refresh/

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
