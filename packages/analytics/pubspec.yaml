name: analytics
version: "0.0.1-dev.3"
description: |
  Interface for working with analytic services. The library is supposed to unify work with various analytic services.
  
repository: "https://github.com/surfstudio/SurfGear/tree/dev/packages/analytics"

dependencies:
  flutter:
    sdk: flutter
  logger: ^0.9.2
  surf_lint_rules: "^0.0.1-dev.0"
  pedantic: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.6.0 <3.0.0"

flutter:

custom:
  is_stable: false
  unstable_version:
  is_plugin: false
  path: analytics
  separate_repo_url:
  hostUrl:
