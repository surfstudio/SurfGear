name: permission
version: "0.0.1-dev.2"
description: Library for requesting and checking permissions
repository: "https://github.com/surfstudio/SurfGear/tree/dev/packages/permission"

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^5.0.1
  surf_lint_rules: "^0.0.1-dev.0"

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.6.0 <3.0.0"

flutter:

custom:
  is_stable: false
  unstable_version:
  is_plugin: false
  path: permission
  separate_repo_url:
  hostUrl:
