name: background_worker
version: "0.0.1-dev.4"
description: A new Flutter project.
repository: "https://github.com/surfstudio/SurfGear/tree/dev/packages/background_worker"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  surf_lint_rules: "^0.0.1-dev.0"

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.6.0 <3.0.0"

flutter:
  uses-material-design: true

custom:
  is_stable: false
  unstable_version:
  is_plugin: false
  path: background_worker
  separate_repo_url:
  hostUrl:
