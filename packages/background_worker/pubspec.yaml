name: background_worker
description: A new Flutter project.
version: 0.0.1-dev.0

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  
  surf_lint_rules:
    path: ../surf_lint_rules

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

custom:
  is_stable: false
  unstable_version: 0
  is_plugin: false
  path: background_worker
  separate_repo_url:
  hostUrl:
