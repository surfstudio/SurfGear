name: background_worker
version: "0.0.1-dev.0"
description: A new Flutter project.

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  surf_lint_rules:
    git:
      url: "https://gitlab.com/surfstudio/public/flutter-standard.git"
      path: packages/surf_lint_rules

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.6.0 <3.0.0"

flutter:
  uses-material-design: true

custom:
  is_stable: false
  unstable_version: 1
  is_plugin: false
  path: background_worker
  separate_repo_url:
  hostUrl:
