name: tabnavigator
version: "0.0.1-dev.8"
description: Module can manage the tabs on the screen.
repository: "https://github.com/surfstudio/SurfGear/tree/dev/packages/tabnavigator"

dependencies:
  flutter:
    sdk: flutter

  surf_lint_rules: ^0.0.2-dev.2

  surf_util:
    git:
      url: https://github.com/surfstudio/SurfGear
      path: packages/surf_util
      ref: feature/surf_util_null_safety

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.12.0-0 <3.0.0"

flutter:

custom:
  is_stable: false
  unstable_version:
  is_plugin: false
  path: tabnavigator
  separate_repo_url:
  hostUrl:
