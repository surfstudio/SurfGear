name: db_holder
version: "0.0.1-dev.2"
description: Separator of app logic and src.manager working logic.
repository: "https://github.com/surfstudio/SurfGear/tree/dev/packages/db_holder"

dependencies:
  flutter:
    sdk: flutter
  surf_lint_rules: ^0.0.2-dev.2

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.12.0-0 <3.0.0"

flutter:

custom:
  is_stable: false
  unstable_version:
  is_plugin: false
  path: db_holder
  separate_repo_url:
  hostUrl:
