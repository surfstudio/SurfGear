name: in_app_rate_example
description: Demonstrates how to use the in_app_rate plugin.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  in_app_rate:
    path: ../

dev_dependencies:
  surf_lint_rules: ^1.0.0

flutter:
  uses-material-design: true
