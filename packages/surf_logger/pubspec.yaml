name: surf_logger
version: "0.0.1-dev.5"
description: "Logger for Dart & Flutter"
repository: "https://github.com/surfstudio/SurfGear/tree/dev/packages/logger"

dependencies:
  flutter:
    sdk: flutter
  surf_lint_rules: "^0.0.1-dev.0"
  logger: ^0.9.2
  pedantic: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.6.0 <3.0.0"

flutter:

custom:
  is_stable: false
  unstable_version:
  is_plugin: false
  path: surf_logger
  separate_repo_url:
  hostUrl:
