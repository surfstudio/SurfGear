name: surf_util
version: "0.0.1-dev.0"
description: Common classes and util.
repository: https://github.com/surfstudio/SurfGear/tree/dev/packages/surf_util

dependencies:
  flutter:
    sdk: flutter
  surf_logger:
    path: ../surf_logger
  relation: ^0.0.2
  swipe_refresh:
    git:
      url: "https://gitlab.com/surfstudio/public/flutter-standard.git"
      path: packages/swipe_refresh
  surf_lint_rules:
    git:
      url: "https://gitlab.com/surfstudio/public/flutter-standard.git"
      path: packages/surf_lint_rules

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.6.0 <3.0.0"

flutter:

custom:
  is_stable: false
  unstable_version: 1
  is_plugin: false
  path: surf_util
  separate_repo_url:
  hostUrl:
