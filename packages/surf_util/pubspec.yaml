name: surf_util
description: Common classes and util.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  logger:
    git:
      url: https://gitlab.com/surfstudio/projects/standard/flutter-standard.git
      ref: project-PRACTICE
      path: packages/logger
  relation:
    git:
      url: https://gitlab.com/surfstudio/projects/standard/flutter-standard.git
      ref: project-PRACTICE
      path: packages/relation
  swipe_refresh:
    git:
      url: https://gitlab.com/surfstudio/projects/standard/flutter-standard.git
      ref: project-PRACTICE
      path: packages/swipe_refresh

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

custom:
  is_stable: false
  unstable_version: 0
  is_plugin: false
  path: surf_util
  separate_repo_url:
  hostUrl:
