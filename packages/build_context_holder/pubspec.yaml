name: build_context_holder
version: "0.1.0-dev.0"
description: The BuildContext store received from the last created widget.
repository: "https://github.com/surfstudio/SurfGear/tree/dev/packages/build_context_holder"
issue_tracker: "https://github.com/surfstudio/SurfGear/issues"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  surf_lint_rules: ^1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

flutter:

custom:
  is_stable: false
  unstable_version:
  is_plugin: false
  path: build_context_holder
  separate_repo_url:
  hostUrl:
