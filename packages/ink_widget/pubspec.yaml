name: ink_widget
description: Widget library with ink effect without problems with decoration for child.
version: 1.0.0-dev.0

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  surf_lint_rules:
    git:
      url: https://gitlab.com/surfstudio/public/flutter-standard.git
      path: packages/surf_lint_rules

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

custom:
  is_stable: false
  unstable_version: 0
  is_plugin: false
  path: ink_widget
  hostUrl:name: ink_widget
version: "1.0.0-dev.0"
description: Widget library with ink effect without problems with decoration for child.

dependencies:
  flutter:
    sdk: flutter
  surf_lint_rules:
    git:
      url: "https://gitlab.com/surfstudio/public/flutter-standard.git"
      path: packages/surf_lint_rules

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.6.0 <3.0.0"

flutter:

custom:
  is_stable: false
  unstable_version: 1
  is_plugin: false
  path: ink_widget
  hostUrl:
  separate_repo_url:
