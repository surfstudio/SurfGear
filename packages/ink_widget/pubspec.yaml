name: ink_widget
version: "1.0.0-dev.4"
description: Widget library with ink effect without problems with decoration for child.
repository: "https://github.com/surfstudio/SurfGear/tree/dev/packages/ink_widget"

dependencies:
  flutter:
    sdk: flutter
  surf_lint_rules: "^0.0.1-dev.0"
  pedantic: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.6.0 <3.0.0"

flutter:

custom:
  is_stable: false
  unstable_version:
  is_plugin: false
  path: ink_widget
  hostUrl:
  separate_repo_url:
