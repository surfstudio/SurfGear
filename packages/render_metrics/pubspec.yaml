name: render_metrics
description: library for getting widget metrics.
version: 1.0.0
author:
homepage: https://github.com/surfstudio/render_metrics
repository: https://github.com/surfstudio/render_metrics

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

custom:
  is_stable: true
  unstable_version: 0
  is_plugin: false
  path: render_metrics
  separate_repo_url: https://github.com/surfstudio/render_metrics
  hostUrl: