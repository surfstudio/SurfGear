name: bottom_sheet
version: "0.0.1-dev.2"
description: |
  Flexible bottom sheet with the ability to scroll content even without a list.
repository: "https://github.com/surfstudio/SurfGear/tree/dev/packages/bottom_sheet"

dependencies:
  flutter:
    sdk: flutter
  surf_lint_rules: "^0.0.1-dev.0"

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.6.0 <3.0.0"

flutter:

custom:
  is_stable: false
  unstable_version:
  is_plugin: false
  path: bottom_sheet
  separate_repo_url:
  hostUrl:
