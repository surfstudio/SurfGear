name: geolocation
version: "0.0.1-dev.0"
description: Geolocation service Wrapper
repository: https://github.com/surfstudio/SurfGear/tree/dev/packages/geolocation

publish_to:

dependencies:
  flutter:
    sdk: flutter
  surf_lint_rules: ^0.0.1-dev.0
  geolocator: ^5.2.0
  location_permissions: ^3.0.0+1
  location: ^2.4.0

  rxdart: ^0.24.0
dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.6.0 <3.0.0"

flutter:
  uses-material-design: true

custom:
  is_stable: false
  unstable_version: 1
  is_plugin: false
  path: geolocation
  separate_repo_url:
  hostUrl:
