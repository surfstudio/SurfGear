name: virtual_keyboard
version: "0.0.1-dev.0"
description: Keyboard widget for use in widget tree
repository: https://github.com/surfstudio/SurfGear/tree/dev/packages/virtual_keyboard

dependencies:
  flutter:
    sdk: flutter
  ink_widget: ^1.0.0-dev.0
  surf_lint_rules: ^0.0.1-dev.0

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.6.0 <3.0.0"

flutter:

custom:
  is_stable: false
  unstable_version: 1
  is_plugin: false
  path: virtual_keyboard
  hostUrl:
  separate_repo_url:
