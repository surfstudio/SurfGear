name: relation
version: "1.0.0"
description: the stream representation of the relations of the entities and widget utilities
homepage: "https://github.com/surfstudio/relation"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.26.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  surf_lint_rules: ^1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependency_validator:
  exclude:
    - "example/**"

flutter:

custom:
  is_stable: false
  unstable_version:
  is_plugin: false
  path: relation
  separate_repo_url: "https://github.com/surfstudio/relation"
  hostUrl:
