name: relation
description: the stream representation of the relations of the entities and widget utilities
version: 0.0.2
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  #rx
  rxdart: ^0.23.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  pedantic: ^1.7.0

flutter:

custom:
  is_stable: false
  unstable_version: 0
  is_plugin: false
  path: relation
  separate_repo_url:
  hostUrl:
