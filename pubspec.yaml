name: mwwm
description: >
  Software architecture pattern for teams with different sizes.
  Add possibility to work separately.
  Separate UI, presentation logic and business logic.
version: 0.1.0+1
homepage: https://github.com/surfstudio/mwwm

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  pedantic: ^1.8.0+1

flutter:

custom:
  is_stable: false
  unstable_version: 0
  is_plugin: false
  path: mwwm
  separate_repo_url: https://github.com/surfstudio/mwwm
  hostUrl:
